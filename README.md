## archlinux-bootstrap-termux

aarch64 only...
Needs wget, proot, and pacman.

```sh
wget -O arch-bootstrap.sh "https://github.com/Alto1772/archlinux-bootstrap-termux/raw/main/arch-bootstrap.sh" && ./arch-bootstrap.sh
```
